{
    "response_type": "in_channel",
    "replace_original": true,
    "blocks": [
        {
            "type": "section",
            "text": {
                "text": "Picked <@{{participant}}> for *{{event.name}}*",
                "type": "mrkdwn"
            },
            "fields": [
                {{#if participants_left.0}}
                {
                    "type": "mrkdwn",
                    "text": "*Next picks:*"
                },
                {
                    "type": "mrkdwn",
                    "text": "{{#each participants_left}} <@{{this}}> {{/each}}"
                }
                {{else}}
                {
                    "type": "mrkdwn",
                    "text": "All participants have been picked!\nA new round will start 🚀"
                }
                {{/if}}
            ],
            "block_id": "{{event.id}}"
        }
    ]
}