{
    "channel": "{{channel}}",
    "blocks": [
        {
            "type": "section",
            "text": {
                "text": "Scheduler automatically picked <@{{participant}}> for *{{event.name}}*!\n_Feel free to repick other user by using `/picker repick {{event.id}}`_",
                "type": "mrkdwn"
            },
            "block_id": "{{event.id}}"
        }
    ]
}